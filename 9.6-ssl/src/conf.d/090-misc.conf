# tested on v9.5

# For historical reasons, this variable contains two independent components:
# - the output format specification (ISO, Postgres, SQL, or German) and
# - the input/output specification for year/month/day ordering (DMY, MDY, or YMD).
#
datestyle = 'iso, ymd'
#datestyle = 'iso, dmy'

